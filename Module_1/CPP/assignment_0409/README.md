1. palindrome check -> to run the program 
```bash
g++ src/Palindrome.cpp -o ./build/Palindrome.out
```
then run 

```bash
./build/Palindrome.out\
```

1. validParanthesis check -> to run the program 

```bash
g++ src/ValidParenthesis.cpp -o ./build/ValidParenthesis.out
```
then run 

```bash
./build/ValidParenthesis.out
```

1. Employee class using map -> to run the program 

```bash
g++ src/Employee.cpp -o ./build/Employee.out
```
then run 

```bash
./build/Employee.out
```